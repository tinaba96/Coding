n = int(input())
point = int(format((n / 5), ".0f"))
print(point * 5)

'''
`format()`関数のフォーマット指定子 `".0f"` を使用すると、小数点以下の桁数を0とするフォーマットが指定されます。このフォーマットは、浮動小数点数を整数部分のみの文字列として表示する効果があります。

具体的な動作は以下のようになります。

1. 小数点以下の桁数を0とするフォーマット `".0f"` を指定します。
2. 浮動小数点数の値を与えられた場合、 `format()`関数はその値を四捨五入して最も近い整数に変換します。
3. 小数点以下の桁数を0とした場合、変換された整数部分のみの文字列が返されます。

例えば、`x = 3.14159` という浮動小数点数を `format(x, ".0f")` でフォーマットすると、小数点以下の桁数を0としたため、値が四捨五入されて `3` という整数部分のみの文字列が返されます。

このように、フォーマット指定子 `".0f"` を使用することで、浮動小数点数を四捨五入した整数部分の文字列として表示することができます。
'''

